server:
  port: 8080
  servlet:
     context-path: /testeproject

spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password: pwd

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  # H2
  #spring.h2.console.enabled=true
  #spring.h2.console.path=/h2

  #spring.datasource.url=jdbc:h2:mem:paribasdb;INIT=runscript from '~/create.sql'\\;runscript from '~/init.sql';
  #spring.datasource.schema=classpath:create.sql
  #spring.datasource.url=jdbc:h2:file:~/paribasdb;INIT=runscript from '~/create.sql'\\;runscript from '~/init.sql';
  #spring.datasource.driverClassName=org.h2.Driver
  #spring.datasource.username=sa
  #spring.datasource.password=pwd
  #spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
  #spring.datasource.initialize=true
#spring.jpa.hibernate.ddl-auto=update