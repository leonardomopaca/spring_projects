server:
  port: 8080
  servlet:
    context-path: /teste
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:paribasdb
    username: sa
    password: pwd
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: none
